import { blockRenderer, entityDecorator } from './custom'
import DraftRenderer from './draft-renderer'

const MirrorMedia = {
  DraftRenderer,
  blockRenderer,
  entityDecorator,
}

export default MirrorMedia
